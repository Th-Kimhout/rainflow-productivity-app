"use client";

import { SettingsView } from "@/components/settings/settings-view";

/** §7.2 backup and sync health. Everything here reads from Dexie, so it works offline too. */
export default function SettingsPage() {
  return <SettingsView />;
}
